import { motion } from 'framer-motion';\nimport { useInView } from '../hooks/useInView';\nimport { useState } from 'react';\n\ninterface Skill {\n  name: string;\n  level: number;\n  category: 'Technical' | 'Professional';\n  description: string;\n  icon: string;\n}\n\nconst SkillsVisualization = () => {\n  const [ref, inView] = useInView({ threshold: 0.3, triggerOnce: true });\n  const [hoveredSkill, setHoveredSkill] = useState<Skill | null>(null);\n\n  const skills: Skill[] = [\n    {\n      name: \"AI/ML\",\n      level: 90,\n      category: \"Technical\",\n      description: \"Deep learning, computer vision, NLP, and intelligent systems development\",\n      icon: \"🤖\"\n    },\n    {\n      name: \"Java\",\n      level: 85,\n      category: \"Technical\",\n      description: \"Enterprise applications, Spring framework, and scalable backend systems\",\n      icon: \"☕\"\n    },\n    {\n      name: \"AWS\",\n      level: 80,\n      category: \"Technical\",\n      description: \"Cloud architecture, Lambda, DynamoDB, and serverless applications\",\n      icon: \"☁️\"\n    },\n    {\n      name: \"Flutter\",\n      level: 95,\n      category: \"Technical\",\n      description: \"Cross-platform mobile development with beautiful, native interfaces\",\n      icon: \"📱\"\n    },\n    {\n      name: \"Public Speaking\",\n      level: 95,\n      category: \"Professional\",\n      description: \"Conference presentations, technical talks, and team leadership\",\n      icon: \"🎤\"\n    },\n    {\n      name: \"Team Work\",\n      level: 90,\n      category: \"Professional\",\n      description: \"Collaborative development, agile methodologies, and cross-functional teams\",\n      icon: \"🤝\"\n    },\n    {\n      name: \"Problem Solving\",\n      level: 85,\n      category: \"Professional\",\n      description: \"Analytical thinking, system design, and innovative solution development\",\n      icon: \"🧩\"\n    },\n    {\n      name: \"Time Management\",\n      level: 80,\n      category: \"Professional\",\n      description: \"Project planning, deadline management, and efficient workflow optimization\",\n      icon: \"⏰\"\n    }\n  ];\n\n  const technicalSkills = skills.filter(skill => skill.category === 'Technical');\n  const professionalSkills = skills.filter(skill => skill.category === 'Professional');\n\n  const getSkillPosition = (index: number, total: number, radius: number) => {\n    const angle = (index * 2 * Math.PI) / total;\n    return {\n      x: Math.cos(angle) * radius,\n      y: Math.sin(angle) * radius\n    };\n  };\n\n  return (\n    <section \n      ref={ref}\n      className=\"py-32 bg-gradient-to-b from-near-black to-deep-black relative overflow-hidden\"\n    >\n      {/* Radar grid background */}\n      <div className=\"absolute inset-0 flex items-center justify-center opacity-5\">\n        <svg width=\"800\" height=\"800\" viewBox=\"0 0 800 800\">\n          {[100, 200, 300, 400].map((radius) => (\n            <circle\n              key={radius}\n              cx=\"400\"\n              cy=\"400\"\n              r={radius}\n              fill=\"none\"\n              stroke=\"#00d4ff\"\n              strokeWidth=\"1\"\n            />\n          ))}\n          {[0, 45, 90, 135, 180, 225, 270, 315].map((angle) => (\n            <line\n              key={angle}\n              x1=\"400\"\n              y1=\"400\"\n              x2={400 + 400 * Math.cos((angle * Math.PI) / 180)}\n              y2={400 + 400 * Math.sin((angle * Math.PI) / 180)}\n              stroke=\"#00d4ff\"\n              strokeWidth=\"1\"\n            />\n          ))}\n        </svg>\n      </div>\n\n      <div className=\"section-container relative z-10\">\n        <motion.div\n          className=\"text-center mb-20\"\n          initial={{ opacity: 0, y: 50 }}\n          animate={inView ? { opacity: 1, y: 0 } : {}}\n          transition={{ duration: 0.8 }}\n        >\n          <motion.div\n            className=\"inline-flex items-center gap-3 mb-6 px-6 py-3 glass-card rounded-full\"\n            whileHover={{ scale: 1.05 }}\n          >\n            <div className=\"w-3 h-3 bg-neon-blue rounded-full animate-pulse\" />\n            <span className=\"text-neon-blue font-mono text-sm tracking-wider\">SKILL MATRIX</span>\n          </motion.div>\n          \n          <h2 className=\"text-4xl lg:text-6xl font-bold mb-6\">\n            <span className=\"text-white\">Technical</span>\n            <span className=\"text-lavender\"> Mastery</span>\n          </h2>\n          \n          <p className=\"text-xl text-text-secondary max-w-3xl mx-auto leading-relaxed\">\n            A comprehensive skill set spanning cutting-edge technologies and essential professional capabilities.\n          </p>\n        </motion.div>\n\n        <div className=\"grid lg:grid-cols-2 gap-16 items-center\">\n          {/* Left: Skill Orbs Visualization */}\n          <motion.div\n            className=\"relative h-96 flex items-center justify-center\"\n            initial={{ opacity: 0, scale: 0.8 }}\n            animate={inView ? { opacity: 1, scale: 1 } : {}}\n            transition={{ delay: 0.3, duration: 0.8 }}\n          >\n            {/* Technical Skills Orbit */}\n            <div className=\"absolute inset-0 flex items-center justify-center\">\n              {technicalSkills.map((skill, index) => {\n                const position = getSkillPosition(index, technicalSkills.length, 120);\n                return (\n                  <motion.div\n                    key={skill.name}\n                    className=\"absolute cursor-pointer group\"\n                    style={{\n                      left: `calc(50% + ${position.x}px)`,\n                      top: `calc(50% + ${position.y}px)`,\n                      transform: 'translate(-50%, -50%)'\n                    }}\n                    initial={{ scale: 0, opacity: 0 }}\n                    animate={inView ? { scale: 1, opacity: 1 } : {}}\n                    transition={{ delay: 0.5 + index * 0.1, duration: 0.6 }}\n                    whileHover={{ scale: 1.2 }}\n                    onHoverStart={() => setHoveredSkill(skill)}\n                    onHoverEnd={() => setHoveredSkill(null)}\n                  >\n                    <div \n                      className=\"w-16 h-16 rounded-full bg-gradient-to-br from-neon-blue/30 to-neon-purple/30 border-2 border-neon-blue/50 flex items-center justify-center backdrop-blur-sm group-hover:border-neon-blue transition-all duration-300\"\n                      style={{\n                        boxShadow: `0 0 ${skill.level / 5}px rgba(0, 212, 255, 0.5)`\n                      }}\n                    >\n                      <span className=\"text-2xl\">{skill.icon}</span>\n                    </div>\n                    <div className=\"absolute -bottom-8 left-1/2 transform -translate-x-1/2 text-xs text-neon-blue font-semibold whitespace-nowrap\">\n                      {skill.name}\n                    </div>\n                  </motion.div>\n                );\n              })}\n            </div>\n\n            {/* Professional Skills Orbit */}\n            <div className=\"absolute inset-0 flex items-center justify-center\">\n              {professionalSkills.map((skill, index) => {\n                const position = getSkillPosition(index, professionalSkills.length, 180);\n                return (\n                  <motion.div\n                    key={skill.name}\n                    className=\"absolute cursor-pointer group\"\n                    style={{\n                      left: `calc(50% + ${position.x}px)`,\n                      top: `calc(50% + ${position.y}px)`,\n                      transform: 'translate(-50%, -50%)'\n                    }}\n                    initial={{ scale: 0, opacity: 0 }}\n                    animate={inView ? { scale: 1, opacity: 1 } : {}}\n                    transition={{ delay: 0.8 + index * 0.1, duration: 0.6 }}\n                    whileHover={{ scale: 1.2 }}\n                    onHoverStart={() => setHoveredSkill(skill)}\n                    onHoverEnd={() => setHoveredSkill(null)}\n                  >\n                    <div \n                      className=\"w-14 h-14 rounded-full bg-gradient-to-br from-neon-purple/30 to-alexa-blue/30 border-2 border-neon-purple/50 flex items-center justify-center backdrop-blur-sm group-hover:border-neon-purple transition-all duration-300\"\n                      style={{\n                        boxShadow: `0 0 ${skill.level / 6}px rgba(139, 92, 246, 0.5)`\n                      }}\n                    >\n                      <span className=\"text-xl\">{skill.icon}</span>\n                    </div>\n                    <div className=\"absolute -bottom-8 left-1/2 transform -translate-x-1/2 text-xs text-neon-purple font-semibold whitespace-nowrap\">\n                      {skill.name}\n                    </div>\n                  </motion.div>\n                );\n              })}\n            </div>\n\n            {/* Central Hub */}\n            <motion.div\n              className=\"w-20 h-20 rounded-full bg-gradient-to-br from-neon-blue/20 to-neon-purple/20 border-2 border-white/20 flex items-center justify-center backdrop-blur-sm\"\n              initial={{ scale: 0 }}\n              animate={inView ? { scale: 1 } : {}}\n              transition={{ delay: 1.2, duration: 0.6 }}\n            >\n              <span className=\"text-2xl\">⚡</span>\n            </motion.div>\n          </motion.div>\n\n          {/* Right: Skill Details */}\n          <motion.div\n            className=\"space-y-6\"\n            initial={{ opacity: 0, x: 50 }}\n            animate={inView ? { opacity: 1, x: 0 } : {}}\n            transition={{ delay: 0.5, duration: 0.8 }}\n          >\n            {hoveredSkill ? (\n              <motion.div\n                className=\"glass-card p-8 rounded-2xl\"\n                initial={{ opacity: 0, y: 20 }}\n                animate={{ opacity: 1, y: 0 }}\n                transition={{ duration: 0.3 }}\n              >\n                <div className=\"flex items-start gap-4 mb-4\">\n                  <div className=\"text-4xl\">{hoveredSkill.icon}</div>\n                  <div>\n                    <h3 className=\"text-2xl font-bold text-white mb-2\">{hoveredSkill.name}</h3>\n                    <span className={`px-3 py-1 text-xs rounded-full ${\n                      hoveredSkill.category === 'Technical' \n                        ? 'bg-neon-blue/20 text-neon-blue border border-neon-blue/30'\n                        : 'bg-neon-purple/20 text-neon-purple border border-neon-purple/30'\n                    }`}>\n                      {hoveredSkill.category}\n                    </span>\n                  </div>\n                </div>\n                \n                <p className=\"text-text-secondary leading-relaxed mb-6\">\n                  {hoveredSkill.description}\n                </p>\n                \n                {/* Skill Level Visualization */}\n                <div className=\"mb-4\">\n                  <div className=\"flex justify-between items-center mb-2\">\n                    <span className=\"text-sm text-text-muted\">Proficiency Level</span>\n                    <span className={`font-bold ${\n                      hoveredSkill.category === 'Technical' ? 'text-neon-blue' : 'text-neon-purple'\n                    }`}>\n                      {hoveredSkill.level}%\n                    </span>\n                  </div>\n                  <div className=\"w-full bg-white/10 rounded-full h-2 overflow-hidden\">\n                    <motion.div\n                      className={`h-full rounded-full ${\n                        hoveredSkill.category === 'Technical' \n                          ? 'bg-gradient-to-r from-neon-blue to-neon-purple'\n                          : 'bg-gradient-to-r from-neon-purple to-alexa-blue'\n                      }`}\n                      initial={{ width: 0 }}\n                      animate={{ width: `${hoveredSkill.level}%` }}\n                      transition={{ duration: 1, ease: \"easeOut\" }}\n                    />\n                  </div>\n                </div>\n              </motion.div>\n            ) : (\n              <motion.div\n                className=\"glass-card p-8 rounded-2xl text-center\"\n                initial={{ opacity: 1 }}\n                animate={{ opacity: 1 }}\n              >\n                <div className=\"text-6xl mb-4\">🎯</div>\n                <h3 className=\"text-xl font-semibold text-white mb-4\">Interactive Skill Matrix</h3>\n                <p className=\"text-text-secondary leading-relaxed\">\n                  Hover over any skill orb to explore detailed proficiency levels and experience. \n                  Inner orbit represents technical skills, outer orbit shows professional capabilities.\n                </p>\n              </motion.div>\n            )}\n\n            {/* Skill Categories Summary */}\n            <div className=\"grid grid-cols-2 gap-4\">\n              <motion.div\n                className=\"glass-card p-4 rounded-xl text-center\"\n                initial={{ opacity: 0, y: 20 }}\n                animate={inView ? { opacity: 1, y: 0 } : {}}\n                transition={{ delay: 1, duration: 0.6 }}\n              >\n                <div className=\"text-2xl text-neon-blue mb-2\">{technicalSkills.length}</div>\n                <div className=\"text-sm text-text-secondary\">Technical Skills</div>\n              </motion.div>\n              \n              <motion.div\n                className=\"glass-card p-4 rounded-xl text-center\"\n                initial={{ opacity: 0, y: 20 }}\n                animate={inView ? { opacity: 1, y: 0 } : {}}\n                transition={{ delay: 1.1, duration: 0.6 }}\n              >\n                <div className=\"text-2xl text-neon-purple mb-2\">{professionalSkills.length}</div>\n                <div className=\"text-sm text-text-secondary\">Professional Skills</div>\n              </motion.div>\n            </div>\n          </motion.div>\n        </div>\n      </div>\n    </section>\n  );\n};\n\nexport default SkillsVisualization;
